# docker-compose.yml
version: "3"
services:
  app:
    build: .
    depends_on:
      - postgres
    environment:
      DATABASE_URL: postgres://mrjtvhysvqlzpi:b1ee8176480eef236bb7f520a3136cf2db3588eaffc74d0c9f5b2bb0460a6cff@ec2-18-213-176-229.compute-1.amazonaws.com:5432/d9m4h3sa0v8q2h?ssl=true
     # DATABASE_URL: postgres://user:pass@postgres:5432/db
      NODE_ENV: development
      PORT: 3000
    ports:
      - "3000:3000"
    command: npm run dev
    volumes:
      - .:/app/
      - /app/node_modules

  postgres:
    image: postgres:10.4
    ports:
      - "35432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: db
